import { IdCardDirective } from './id-card.directive';

describe('IdCardDirective', () => {
  it('should create an instance', () => {
    const directive = new IdCardDirective();
    expect(directive).toBeTruthy();
  });
});
