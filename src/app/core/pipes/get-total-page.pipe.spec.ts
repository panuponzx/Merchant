import { GetTotalPagePipe } from './get-total-page.pipe';

describe('GetTotalPagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTotalPagePipe();
    expect(pipe).toBeTruthy();
  });
});
