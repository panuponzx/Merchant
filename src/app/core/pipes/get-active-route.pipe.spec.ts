import { GetActiveRoutePipe } from './get-active-route.pipe';

describe('GetActiveRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new GetActiveRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
