import { GetRoutePathPipe } from './get-route-path.pipe';

describe('GetRoutePathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRoutePathPipe();
    expect(pipe).toBeTruthy();
  });
});
