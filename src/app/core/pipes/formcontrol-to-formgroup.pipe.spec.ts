import { FormcontrolToFormgroupPipe } from './formcontrol-to-formgroup.pipe';

describe('FormcontrolToFormgroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FormcontrolToFormgroupPipe();
    expect(pipe).toBeTruthy();
  });
});
