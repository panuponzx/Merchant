import { FormarrayToFormgroupPipe } from './formarray-to-formgroup.pipe';

describe('FormarrayToFormgroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FormarrayToFormgroupPipe();
    expect(pipe).toBeTruthy();
  });
});
