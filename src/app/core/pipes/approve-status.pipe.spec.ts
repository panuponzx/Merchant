import { ApproveStatusPipe } from './approve-status.pipe';

describe('ApproveStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ApproveStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
